{
  "name": "clinica_saude_mais-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Root for frontend and backend projects",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm:dev --prefix backend\" \"npm:dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
